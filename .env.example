# OpenFlow Builder - Environment Configuration
# Copy this file to .env and update values as needed
# The launch.sh script will automatically generate .env if it doesn't exist

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# For local MySQL:
DATABASE_URL="mysql://root:openflow@localhost:3306/openflow_builder"

# For remote MySQL:
# DATABASE_URL="mysql://user:password@remote-host:3306/database_name"

# ============================================================================
# AUTHENTICATION & SECURITY
# ============================================================================
# JWT secret for session tokens (auto-generated, can override)
JWT_SECRET="your-secret-key-here-min-32-characters-recommended"

# OAuth server URL (for authentication flow)
OAUTH_SERVER_URL="http://localhost:3000/api/oauth"

# Owner's Open ID (admin user)
OWNER_OPEN_ID="dev-user"

# ============================================================================
# APPLICATION SETTINGS
# ============================================================================
# Environment: development, staging, production
NODE_ENV="development"

# Application ID (used in OAuth flow)
VITE_APP_ID="openflow-builder-local"

# Server port
PORT=3000

# ============================================================================
# OPTIONAL: AI SERVICES
# ============================================================================
# Uncomment and add your API keys for AI features

# Google Gemini API
# GEMINI_API_KEY="your-gemini-api-key"

# For other AI providers, configure via UI after startup

# Forge API (internal AI integration)
# BUILT_IN_FORGE_API_URL="https://api.example.com"
# BUILT_IN_FORGE_API_KEY="your-forge-api-key"

# ============================================================================
# OPTIONAL: AWS S3 (for file uploads)
# ============================================================================
# AWS_ACCESS_KEY_ID="your-aws-access-key"
# AWS_SECRET_ACCESS_KEY="your-aws-secret-key"
# AWS_REGION="us-east-1"
# AWS_S3_BUCKET="your-bucket-name"

# ============================================================================
# OPTIONAL: EXTERNAL SERVICES
# ============================================================================
# Discord notifications (if implemented)
# DISCORD_WEBHOOK_URL=""

# Slack notifications (if implemented)
# SLACK_WEBHOOK_URL=""

# ============================================================================
# NOTES FOR DEPLOYMENT
# ============================================================================
# - launch.sh will auto-generate .env with sensible defaults
# - Never commit .env to git (it's in .gitignore)
# - For production, use secure secret management (AWS Secrets Manager, etc.)
# - All required variables have defaults; optional ones can be omitted
# - Database must be running before starting the application
